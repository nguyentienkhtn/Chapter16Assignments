/**
   The ObjectBinarySearcher class provides a public
   method for performing a binary search on an array of
   Comparable objects.
*/

public class ObjectBinarySearcher
{
   /**
      The search method performs a binary search on an 
      array of Comparable objects. The array is searched
      for the object passed to value. If the object is 
      found, its array subscript is returned. Otherwise, 
      -1 is returned indicating the object was not found 
      in the array.
      @param array The array to search.
      @param value The object to search for.
   */

   public int search(array, value)
   {
      /***
       * you should modify the method parameter list and
       * the method body as well
       */
   }

}